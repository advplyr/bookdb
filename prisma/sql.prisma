generator client {
  provider = "prisma-client-js"
  output   = "./generate/sqlclient"
}

datasource db {
  provider = "mysql"
  url      = env("SQL_DB_URL")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
